extends layout

block content

	div.genre-detail
		h1 #{title} - "#{genre.name}"

		if genre_books.length

			p Delete the following books before deleting this genre.

			each book in genre_books
				ul
					li.books-genre-link
						a(href=book.url) #{book.title}

		else
			p Are you sure you want to delete this Genre?

			form(method='POST' action='')
				input(type='hidden' name='id' value=genre._id)
				button.deleteBtn(type='submit') Delete
				a(href=genre.url)
					button.goBackBtn(type='button') Go back